super + F12
	pkill -USR1 -x sxhkd

alt + F12
  urxvt -e mx upd

control + F12
  tmux-kill-detached

control + ~c
	browser-doubleyank

alt + ~c
	browser-unbookmark

super + space
	firefox-ulm

super + F1
	ship

alt + F2
	main-dmenu
