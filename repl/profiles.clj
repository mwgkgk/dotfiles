{:user 
 {:dependencies
  [[cljfmt "0.6.4"]

   ;; Provides:
   ;; - (load-project) to load new deps from project.clj
   ;; - (alembic.still/distill '[org.clojure/tools.logging "0.3.2"])
   [alembic "0.3.2"]]}}
