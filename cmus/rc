cd ~/music/
view library

colorscheme gruvbox-alt
set lib_sort=filename

bind -f common q :q
bind -f common ^L :vol +5%
bind -f common ^H :vol -5%

set output_plugin=alsa
set dsp.alsa.device=default
set mixer.alsa.device=default
set softvol=true
