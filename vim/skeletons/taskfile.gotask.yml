# https://github.com/go-task/task

b:
  desc: Cargo Build
  cmds:
    - cargo build

t:
  desc: Cargo Test
  cmds:
    - cargo test --  --nocapture

i:
  desc: Cargo Build Release & Install
  cmds:
    - cargo build --release
    # - cp -i ./target/release/exec ~/bin/
